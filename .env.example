# SoilProve does not require any environment variables for live data.
# USDA SDA, NWS, and the US Census geocoder are public and unauthenticated.
#
# Optional: identify your deployment to the NWS API (good citizenship at scale).
# Used by src/lib/integrations/nws.ts if set.
# NWS_USER_AGENT="YourApp (contact@example.com)"
